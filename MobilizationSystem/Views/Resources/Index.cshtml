@using MobilizationSystem.Domain
@model IEnumerable<Resource>
@{
    ViewData["Title"] = "Resources";
}

<h2>Resources</h2>

<a asp-action="Create" class="btn btn-primary mb-2">Add Resource</a>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Name</th>
            <th>Type</th>
            <th>Available</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var p in Model)
{
        <tr>
            <td>@p.Name</td>
            <td>@p.Type</td>
            <td>@p.IsAvailable</td>
            <td>
                <a asp-action="Edit" asp-route-id="@p.Id" class="btn btn-sm btn-warning">Edit</a>
            </td>
        </tr>
}
    </tbody>
</table>
